# DSH Vision Bench 产品与功能规划

> 文档状态：初版规划（2026-08 更新进度标注）  
> 目标产品：`dsh-vision-bench`  
> 目标运行平台：Windows  
> 当前开发环境：macOS，仅用于开发与验证，不属于正式产品范围  
>
> **进度速览（v0.15.0）**：
> - M1 共享台架基础 ✅（任务模型/时间线/会话绑定/总览页）
> - M2 调试工作台 ✅（结构化错误/日志查看器/产物哈希/OpenOCD 烧录确认卡/串口监视）
> - M3 上位机工作台 ✅（点表元数据+CSV/趋势曲线/阈值告警/受控写点+回读）
> - M4 深度 Session 联动 ✅（主动通知/Agent 写点硬确认卡/人工操作请求卡）
> - M5 本地闭环与 Windows 正式版 ◐（台架自检 ✅；验证流程、首次配置向导、Markdown 报告未做）
>
> 以下为原始规划文本，里程碑版本号建议与实际发布节奏已不一致，以本速览为准。

## 1. 文档目的

本文档定义 `dsh-vision-bench` 后续一段时间内的产品定位、功能范围、交互模型、Session Agent 联动方式、阶段目标和验收标准。

规划只面向以下产品形态：

- 在一台 Windows 电脑上部署 DeepSeek Harness 和本插件。
- 连接一套本地嵌入式或工业控制台架。
- 用户既可以使用传统调试界面和上位机界面，也可以让当前 Session 的「台架模式」Agent 参与工作。
- Keil、OpenOCD、Python、串口和 Modbus 等能力均由本机直接提供。

本文档不规划远程 Runner、多人共享、设备市场或跨行业平台化能力。

---

## 2. 产品定位

`dsh-vision-bench` 不是一个单纯的编译按钮集合，也不是一个只靠对话操作设备的 AI 工具。

它的定位是：

> 面向嵌入式与工业控制开发的本地 AI 台架工作站，让传统调试、上位机监控和当前 Session Agent 围绕同一份现场状态协同工作。

插件需要同时满足两种使用方式：

1. **不使用 Agent 时**，它是一套完整、直观、可靠的传统调试界面和上位机界面。
2. **使用台架模式 Agent 时**，Agent 能理解当前工程、设备、连接、日志、测点和最近操作，并能参与编译、诊断、验证和测试流程。

Agent 是台架的协作者，而不是传统界面的替代品。

---

## 3. 产品目标

### 3.1 核心目标

- 统一管理当前工作区、Keil 工程、Target、固件产物和本机工具绑定。
- 提供传统的编译、下载、日志查看、Modbus 监控和数据趋势能力。
- 建立调试界面、上位机界面与当前 Session Agent 的双向联动。
- 让 Agent 基于真实现场状态和证据工作，而不是猜测工程、设备和结果。
- 形成“修改代码 → 编译 → 下载 → 运行 → 观察 → 判断”的本地闭环。
- 对所有重要操作保留来源、时间、输入、结果和关联 Session。

### 3.2 成功标准

当插件达到可正式使用状态时，用户应能完成以下工作：

1. 在 Windows 上绑定并检查 Keil、Python、OpenOCD 等本机程序。
2. 打开一个工程工作区，选择 Keil 工程和 Target。
3. 在传统界面中完成编译、查看错误、选择产物和下载固件。
4. 在上位机界面中查看点表、实时数值、趋势曲线和通信状态。
5. 将错误、日志、点位或曲线区间发送给当前 Session Agent。
6. 由 Agent 发起编译、读取、诊断或验证，并在界面中看到全过程。
7. 对写点、下载、复位等高影响操作进行人工确认。
8. 回看一次问题从发现、分析到验证完成的完整证据链。

---

## 4. 明确不做的内容

当前规划明确排除以下方向：

- macOS 正式部署支持。
- 远程 Windows Runner 或跨机器执行。
- 多台设备拓扑管理。
- 多人共享台架、预约和权限租约。
- CAN/DBC。
- 设备模板市场或公共配置市场。
- 暖通、空调、储能等独立行业知识包。
- 云端台架管理。
- 团队级测试管理平台。
- 用聊天界面完全取代传统调试界面和上位机界面。

这些内容在当前阶段不预留复杂架构，不作为验收条件，也不影响本地单机版本发布。

> **例外说明（2026-08）**：「多台设备拓扑管理」指远程/多从站组网类能力，仍不在规划内。但本机 TCP 从机仿真与主从示例（`bench-devices` / `bench-slave`）在规划定稿前就已存在，是开发环境（macOS）上验证写点、回读与总线报文的唯一手段，予以保留；Windows 正式验收仍以单台架 + 真实设备为准。

---

## 5. 核心设计原则

### 5.1 一份现场，多种入口

调试界面、上位机界面和 Agent 必须读取同一份台架状态，不允许各自维护互相冲突的工程、设备和连接信息。

### 5.2 传统操作始终可用

所有核心功能都必须可以由用户直接在界面中完成。Agent 失效、模型不可用或用户不想使用 Agent 时，插件仍然可以正常完成日常调试工作。

### 5.3 Agent 双向联动

联动包括四个方向：

- 界面状态可以被 Agent 查询。
- 重要现场变化可以主动通知当前 Session Agent。
- 用户可以把选中的界面内容发送给 Agent。
- Agent 的操作、进度和结论可以回到界面中展示。

### 5.4 不把实时数据全部塞进提示词

Agent 的现场认知分为三层：

1. 预设提供稳定、简短的台架工作规则。
2. 重要变化以结构化事件通知当前 Session。
3. 大体量日志、曲线和测点数据由 Agent 按需调用工具查询。

### 5.5 人在回路

读取、查询和诊断可以自动执行；写点、下载固件、复位设备等操作必须让用户清楚地看到目标和影响，并在需要时确认。

### 5.6 所有结论都能追溯证据

Agent 的诊断应关联到具体编译记录、日志区间、测点快照、曲线区间或操作结果，不能只保留一段脱离现场的文字结论。

---

## 6. 整体功能结构

插件最终由五个用户可感知的功能区域组成。

### 6.1 台架总览

台架总览是用户和 Agent 理解当前现场的统一入口。

展示内容包括：

- 当前工作区。
- 当前 Keil 工程与 Target。
- 当前 Git 分支和工作区变更提示。
- Python、Keil UV4、OpenOCD 可用状态。
- 当前调试器和设备连接状态。
- 最近一次编译结果。
- 当前选择的固件产物及其生成时间。
- Modbus 连接状态。
- 串口连接状态。
- 当前 Session 与台架模式 Agent 的绑定状态。
- 最近重要事件和待确认操作。

总览提供以下快捷入口：

- 打开调试页。
- 打开上位机页。
- 查看最近失败。
- 发送当前台架快照给 Agent。
- 让 Agent 检查当前现场。
- 处理 Agent 发出的人工操作请求。

### 6.2 调试界面

调试界面负责工程、编译、固件和设备下载流程。

主要区域：

- 工程与 Target。
- 编译控制与结果。
- 错误和警告列表。
- 固件产物。
- 下载与验证。
- 串口日志。
- 调试操作记录。

### 6.3 上位机界面

上位机界面负责 Modbus 连接、点位监控、趋势查看和受控写入。

主要区域：

- 连接配置和健康状态。
- 点表与分组。
- 实时数据表格。
- 趋势曲线。
- 告警与异常标记。
- 写点和回读验证。
- 数据快照与导出。

### 6.4 台架时间线

时间线统一记录用户、Agent、设备和系统产生的重要事件。

主要用途：

- 回看一次问题处理过程。
- 在界面记录和 Session 对话之间互相跳转。
- 识别某个操作由谁发起。
- 为 Agent 诊断提供最近事件。
- 为测试报告提供可追溯证据。

### 6.5 Session Agent 联动层

联动层不单独替代业务页面，而是贯穿总览、调试、上位机和时间线。

它负责：

- 当前 Session 绑定。
- 预设 Agent 能力装配。
- 工具查询和操作。
- 重要事件通知。
- 界面选区发送。
- Agent 操作回显。
- 用户确认和人工步骤。
- Agent 结论挂接到现场证据。

---

## 7. 台架状态与 Session 状态

为了实现正确联动，需要明确区分三类状态。

### 7.1 台架共享状态

台架共享状态代表当前电脑和真实设备的客观情况：

- 当前工作区。
- 当前工程和 Target。
- 本机工具绑定。
- 当前设备连接。
- 当前 Modbus 配置。
- 当前串口配置。
- 最新固件产物。
- 最近编译、下载和设备操作。

它由传统界面和 Agent 共同读取。

### 7.2 当前 Session 关注状态

Session 状态代表当前 Agent 正在处理的任务：

- 当前诊断目标。
- Agent 正在关注的错误、日志或点位。
- 用户发送给 Agent 的曲线区间。
- 当前执行中的验证流程。
- 等待用户完成的人工操作。
- Agent 给出的阶段性结论。

同一个台架状态可以被不同 Session 观察，但只有当前绑定的 Session 接收主动通知和交互请求。

### 7.3 操作任务状态

编译、下载、采样和测试不能只表现为一次按钮点击，需要形成可追踪任务：

- `taskId`。
- 任务类型。
- 发起来源：用户或 Agent。
- 关联 Session。
- 开始时间和结束时间。
- 当前阶段和进度。
- 输入参数。
- 标准输出、错误输出和附件。
- 最终结果。
- 是否等待用户确认。
- 是否被取消或超时。

---

## 8. 传统调试功能规划

### 8.1 工程管理

- 浏览工作区内的 Keil 工程。
- 显示工程名称、路径和最近使用时间。
- 解析并列出 Target。
- 记住每个工作区最后使用的工程和 Target。
- 显示当前 Git 分支和是否存在未提交改动。
- 显示工程使用的 MCU、输出目录和主要构建信息。
- 提供“发送工程信息给 Agent”。

### 8.2 编译

- 支持选择 Target 和构建配置。
- 显示编译进行状态和耗时。
- 结构化解析 errors、warnings、ROM 和 RAM 占用。
- 错误列表支持文件、行号、错误码和消息。
- 支持复制错误、批量选择和发送给 Agent。
- 保存最近若干次编译记录。
- 对比前后两次固件体积。
- 显示编译日志并支持搜索、过滤和导出。
- Agent 发起的编译在界面中明确标记来源。

### 8.3 固件产物

- 识别 HEX、BIN、AXF、ELF 等产物。
- 显示文件路径、大小、生成时间和哈希。
- 标记当前待下载产物。
- 保留最近使用产物记录。
- 编译完成后提示所需格式是否生成。
- 支持将产物信息发送给 Agent。

### 8.4 下载与验证

- 支持通过 Keil 或 OpenOCD 下载固件。
- 下载前展示工程、Target、产物、设备和风险说明。
- 下载前要求用户确认高影响操作。
- 显示下载进度和日志。
- 下载后执行校验。
- 可选执行复位和运行。
- 读取设备固件版本或用户定义的验证点。
- 将“产物版本”和“设备实际版本”进行对比。
- 失败时保留完整上下文供 Agent 分析。

### 8.5 串口日志

- 串口选择、波特率和常用参数配置。
- 连接、断开和自动重连。
- 实时时间戳。
- 暂停显示但不停止采集。
- 关键字过滤和搜索。
- 错误、警告、重启、断言等日志高亮。
- 选中一段日志发送给 Agent。
- Agent 查询最近若干行或指定时间范围日志。
- 日志导出和关联编译、下载记录。

---

## 9. 传统上位机功能规划

### 9.1 Modbus 连接

- 支持 RTU 和 TCP。
- 显示连接状态、最后成功时间和连续失败次数。
- 支持手动连接、断开和重连。
- 保存每个工作区的连接配置。
- 连接失败提供可读诊断。
- 将连接变化记录到台架时间线。

### 9.2 点表

- 支持手工创建点位。
- 支持 CSV 和 JSON 导入导出。
- 点位字段至少包括：
  - 名称。
  - 分组。
  - 从站地址。
  - 功能码。
  - 寄存器地址。
  - 数据类型。
  - 字节序和字序。
  - 倍率和偏移。
  - 单位。
  - 读写属性。
  - 枚举映射。
  - 备注。
- 支持点位收藏和监控分组。
- 支持将点位或分组发送给 Agent。

### 9.3 实时监控

- 批量轮询点位。
- 显示当前值、单位、质量状态和更新时间。
- 区分正常、超时、无响应和协议异常。
- 支持不同点位使用不同刷新周期。
- 支持暂停和恢复采样。
- 支持制作某一时刻的设备状态快照。
- Agent 可按需读取当前值或指定分组。

### 9.4 趋势曲线

- 多点位同图显示。
- 时间范围缩放和拖动。
- 显示最大值、最小值、平均值和变化速率。
- 支持选择一个时间区间。
- 支持将选中区间和相关点位发送给 Agent。
- Agent 可以创建临时关注组和诊断标记。
- Agent 的结论可以绑定到具体曲线区间。
- 支持导出选中区间数据。

### 9.5 写点

- 只允许对标记为可写的点位操作。
- 写入前显示当前值、目标值、单位和范围。
- 对越界值明确阻止。
- 用户直接写入也进入时间线。
- Agent 发起写点必须由用户确认。
- 写入后自动回读。
- 显示写入前值、目标值、回读值和最终状态。
- 批量写入不作为初期功能。

### 9.6 告警与异常

- 点位可配置简单上下限和变化速率阈值。
- 异常在表格和曲线中高亮。
- 重要异常记录到时间线。
- 当前绑定 Session 可接收异常摘要。
- Agent 按需查询异常前后的相关点位和日志。
- 初期只做本地规则，不构建复杂规则引擎。

---

## 10. Session Agent 联动规划

### 10.1 台架模式预设

安装插件后提供「台架模式」预设。该预设应让 Agent 明确知道：

- 它正在一个真实设备台架环境中工作。
- 当前工程、连接和设备状态必须通过工具确认。
- 调试页和上位机页是用户可直接操作的现场界面。
- 界面选择和时间线事件是可靠的用户现场上下文。
- 大量数据应按需查询，不能要求全部进入系统提示。
- 写点、下载、复位等操作需要遵守人工确认规则。
- 诊断结论必须引用实际证据。

预设本身只提供稳定规则和工具能力，不持续塞入高频实时数据。

### 10.2 当前 Session 绑定

界面需要明确展示：

- 当前是否有 Session。
- 当前 Session 是否使用台架模式。
- 当前台架事件将通知哪个 Session。
- 当前是否存在 Agent 正在执行的台架任务。

建议交互：

- 新建并选择台架模式 Session 后自动绑定。
- 用户可以手动将当前 Session 绑定或解绑台架。
- 切换 Session 后，主动通知只发送给新的当前绑定 Session。
- 已经运行的任务继续保留原始发起 Session 归属。

### 10.3 Agent 获取现场信息

Agent 能查询：

- 当前台架总览。
- 当前工程、Target 和产物。
- 最近编译和下载记录。
- 编译错误与完整日志。
- 串口最近日志和指定时间范围。
- Modbus 连接状态。
- 点表、点位当前值和快照。
- 指定曲线时间范围的数据摘要。
- 最近时间线事件。
- 当前等待确认或人工操作的任务。

### 10.4 重要事件主动通知

以下事件可以通知当前绑定 Session：

- 用户切换工程或 Target。
- 编译完成或失败。
- 下载完成或失败。
- 设备连接、断开或重启。
- 串口发现新的严重错误或断言。
- Modbus 连续通信失败。
- 监控点超过已配置阈值。
- 用户向 Agent 发送日志、点位或曲线区间。
- 用户完成 Agent 请求的人工步骤。
- Agent 发起的后台任务完成。

通知只包含摘要和查询线索，不包含完整大数据。

### 10.5 用户从界面发给 Agent

调试界面提供：

- 发送当前工程。
- 发送选中的编译错误。
- 发送完整编译记录。
- 发送选中的串口日志。
- 发送固件产物和下载结果。

上位机界面提供：

- 发送点位。
- 发送监控分组。
- 发送设备状态快照。
- 发送曲线选区。
- 发送异常事件及其前后数据。

发送时应生成结构化引用，而不是只复制一段文本。

### 10.6 Agent 操作回到界面

Agent 发起的操作必须在传统界面中可见：

- 显示操作来源为 Agent。
- 显示关联 Session。
- 显示执行进度。
- 显示参数和结果。
- 支持用户取消。
- 需要确认时在界面出现明确卡片。
- 失败时提供“回到 Session 查看分析”。

Agent 还可以请求界面执行以下呈现行为：

- 打开调试页或上位机页。
- 聚焦某次编译记录。
- 高亮某条错误。
- 高亮某个点位。
- 展示某段曲线范围。
- 创建临时监控分组。
- 展示需要用户执行的人工步骤。

### 10.7 人工操作请求

Agent 不能自动完成的现场动作，应通过结构化请求交给用户：

- 连接或断开硬件。
- 上电或断电。
- 按下复位键。
- 切换拨码开关。
- 确认设备型号或指示灯状态。
- 确认是否允许下载、写点或复位。

请求状态包括：等待、已完成、已拒绝和已取消。用户完成后，Agent 能继续原来的流程。

---

## 11. Agent 工具能力规划

初期继续保留 `vision_bench` 作为统一入口，避免一次安装向模型暴露过多零散工具。能力增多后，再根据 schema 大小和使用效果决定是否拆分。

规划中的能力分组如下。

### 11.1 只读能力

- 查询台架总览。
- 查询当前 Session 绑定状态。
- 列出工作区资源。
- 查询工程和 Target。
- 查询编译记录、错误和日志。
- 查询固件产物。
- 查询设备和连接状态。
- 查询串口日志。
- 查询点表和点位当前值。
- 查询采样数据和曲线摘要。
- 查询时间线事件。

### 11.2 普通操作能力

- 选择工程和 Target。
- 发起编译。
- 取消编译。
- 创建临时监控分组。
- 创建设备状态快照。
- 发起只读验证步骤。
- 请求用户完成现场操作。
- 请求界面聚焦某个对象。

### 11.3 受控高影响能力

- 下载固件。
- 复位设备。
- Modbus 写点。
- 发送可能改变设备状态的串口命令。

这些能力必须返回待确认内容，由用户在界面中批准后执行。

---

## 12. 台架时间线与证据

### 12.1 事件类型

至少记录：

- 工程选择。
- Target 选择。
- 工具绑定变化。
- 编译开始、完成、失败和取消。
- 产物生成。
- 下载开始、完成、失败和取消。
- 设备连接、断开、复位和重启。
- 串口严重日志。
- Modbus 连接变化。
- 点位读取异常。
- 点位写入和回读。
- 曲线区间选择。
- 用户发送现场内容给 Agent。
- Agent 发起操作。
- 用户批准、拒绝或取消。
- Agent 诊断结论。
- 验证步骤通过或失败。

### 12.2 每条事件的公共字段

- 唯一事件 ID。
- 时间。
- 工作区。
- 来源类型和来源名称。
- 关联 Session。
- 关联任务。
- 事件摘要。
- 结构化详细数据。
- 相关附件或日志定位。
- 操作结果。

### 12.3 界面表现

- 按时间倒序展示。
- 支持按编译、下载、通信、Agent 和异常筛选。
- 支持从事件跳转到对应界面对象。
- 支持从事件打开关联 Session。
- 支持选中多条事件发送给 Agent。

---

## 13. 本地测试闭环

在传统功能和 Agent 联动稳定后，增加轻量的本地验证流程，不建设团队级测试平台。

### 13.1 验证步骤

支持以下步骤：

- 编译指定工程和 Target。
- 确认指定格式产物存在。
- 下载固件。
- 等待设备上线。
- 等待串口关键字。
- 读取一个或多个 Modbus 点位。
- 判断点位是否在范围内。
- 持续采样一段时间。
- 请求用户完成一个人工步骤。
- 生成通过或失败结论。

### 13.2 验证流程示例

```text
1. 编译当前 Release Target
2. 确认 HEX 产物生成
3. 请求用户确认下载目标设备
4. 下载固件并复位
5. 等待串口出现启动完成标记
6. 读取设备版本寄存器
7. 对比设备版本与当前固件版本
8. 连续读取关键点位 30 秒
9. 判断通信和数值是否正常
10. 输出带证据的验证结果
```

### 13.3 结果输出

- 每一步开始、结束和耗时。
- 输入参数。
- 实际结果。
- 通过或失败。
- 对应日志、点位和产物。
- Agent 总结。
- 可导出的本地 Markdown 报告。

---

## 14. Windows 部署功能

### 14.1 安装与首次配置

- 检测 Windows 和 Node.js 环境。
- 检查 Python、Keil UV4、OpenOCD 路径。
- 检查 Python 依赖。
- 检查常用串口驱动和可见串口。
- 检查台架模式预设是否可用。
- 提供一页首次配置向导。
- 配置完成后运行一次只读自检。

### 14.2 环境诊断

提供“台架自检”功能，检查：

- 插件 Host 和 Client 是否加载。
- 当前 Session 是否能调用台架工具。
- 本机程序是否存在并可启动。
- Python 依赖是否齐全。
- 工作区是否可访问。
- 串口是否可打开。
- Modbus 连接是否可建立。
- 调试器是否可识别。

诊断结果可以直接发送给 Agent。

### 14.3 本地数据位置

本地数据应清楚分为：

- 全局工具绑定。
- 每工作区设置。
- 点表。
- 编译和下载记录。
- 串口与采样数据。
- 时间线和验证报告。

界面需要提供“打开数据目录”和“清理历史记录”，但不能默认删除工程文件和固件产物。

---

## 15. 典型联动流程

### 15.1 编译失败后由 Agent 修复

```text
用户在调试页点击编译
→ 插件创建编译任务并记录来源
→ 编译失败，界面显示结构化错误
→ 当前 Session 收到失败摘要
→ Agent 查询完整错误和相关日志
→ Agent修改代码
→ Agent 发起重新编译
→ 调试页显示“由 Agent 发起”及进度
→ 编译成功并生成产物
→ Agent给出修改与验证结论
```

### 15.2 用户把曲线异常交给 Agent

```text
用户在上位机曲线上选择异常区间
→ 点击“让 Agent 分析”
→ 插件创建曲线区间引用并发送给当前 Session
→ Agent 查询同期相关点位、串口日志和最近操作
→ Agent 在曲线上添加异常标记
→ Agent 给出可能原因和下一步验证建议
→ 用户决定是否执行验证
```

### 15.3 Agent 请求下载并验证

```text
Agent 完成代码修改和编译
→ Agent 发起下载请求
→ 调试页显示 Target、产物、哈希和目标设备
→ 用户批准
→ 插件下载、校验并复位设备
→ 插件等待启动日志并读取版本
→ Agent 汇总下载和运行验证结果
```

### 15.4 Agent 请求人工现场操作

```text
Agent 判断需要重新上电
→ 界面出现“请断电后重新上电”的任务卡
→ 用户完成并点击确认
→ 事件写入时间线
→ Agent 收到完成通知并继续读取设备状态
```

---

## 16. 版本与里程碑建议

版本号仅作为规划建议，可根据实际开发节奏调整。

### 里程碑 M1：共享台架基础（建议 `0.5.x`）

目标：建立后续传统界面和 Agent 联动都依赖的共同基础。

功能：

- 台架总览初版。
- 明确台架共享状态与当前 Session 状态。
- 当前 Session 绑定显示。
- 统一任务模型。
- 统一时间线初版。
- 现有编译和 Modbus 读取进入任务与时间线。
- Agent 可查询台架状态和最近事件。

验收：

- 用户和 Agent 看到的工程、Target、连接和最近结果一致。
- 用户操作与 Agent 操作都能标记来源。
- 切换 Session 后，主动事件只进入当前绑定 Session。

### 里程碑 M2：调试工作台（建议 `0.6.x`）

目标：让插件能够承担日常 Windows 固件调试工作。

功能：

- 结构化编译错误和警告。
- 编译记录和日志查看。
- 产物管理和哈希。
- Keil/OpenOCD 下载。
- 下载校验和复位。
- 串口日志。
- 调试页中的 Agent 发送入口。
- Agent 编译、查询日志和请求下载。

验收：

- 用户可以完全不使用 Agent 完成编译和下载。
- Agent 可以根据一次真实编译失败获取足够证据进行修复。
- Agent 发起的操作在调试页完整可见并可取消。
- 下载必须经过清晰确认并留下记录。

### 里程碑 M3：上位机工作台（建议 `0.7.x`）

目标：形成可日常使用的 Modbus 上位机。

功能：

- 点表导入、编辑、分组和导出。
- 批量轮询。
- 数据类型、倍率、单位和枚举。
- 实时趋势曲线。
- 状态快照。
- 简单阈值异常。
- 受控写点和回读。
- 点位、快照和曲线区间发送给 Agent。
- Agent 查询当前值、历史区间和异常上下文。

验收：

- 用户可以在一个页面完成连接、监控、趋势和单点写入。
- Agent 能准确引用用户选中的点位和时间区间。
- 写点必须显示写前、目标和回读值。

### 里程碑 M4：深度 Session 联动（建议 `0.8.x`）

目标：让 Agent 真正成为传统界面中的协作者。

功能：

- 重要事件主动通知。
- Agent 操作进度回显。
- 界面对象与 Session 互相跳转。
- Agent 聚焦界面对象。
- 人工操作请求。
- Agent 结论绑定错误、日志、点位或曲线区间。
- 待确认任务统一入口。

验收：

- 编译失败、曲线异常、设备断开等事件能形成完整双向工作流。
- Agent 不需要依赖用户手工复制大段日志或寄存器数据。
- 所有 Agent 结论都能回到对应现场证据。

### 里程碑 M5：本地闭环与 Windows 正式版（建议 `0.9.x → 1.0.0`）

目标：完成稳定的本地“编译—下载—运行—观察—判断”闭环。

功能：

- 轻量验证流程。
- 等待条件和阈值判断。
- 人工确认步骤。
- 验证结果和 Markdown 报告。
- Windows 首次配置向导。
- 台架自检。
- 本地数据管理。
- 稳定升级和预设维护流程。

验收：

- 在目标 Windows 机器上从全新安装完成首次配置。
- 能运行至少一条真实设备验证流程。
- Agent 能在用户监督下完成一次完整闭环。
- 重启 DSH 后工作区、点表和历史记录可以正确恢复。

---

## 17. 验收场景清单

正式版本至少覆盖以下真实场景：

1. 首次安装后创建并选择台架模式 Session。
2. 绑定 Windows Python、Keil UV4 和 OpenOCD。
3. 选择工程和 Target，完成一次成功编译。
4. 产生编译错误，将错误发送给 Agent 并重新编译。
5. 选择固件并完成一次用户确认后的下载和验证。
6. 连接串口并把一段错误日志发送给 Agent。
7. 导入点表并连续监控多个 Modbus 点位。
8. 在曲线上选择一个区间并让 Agent 分析。
9. 由 Agent 发起一次点位读取。
10. 由 Agent 请求一次写点，用户拒绝后不产生设备修改。
11. 由 Agent 请求一次写点，用户批准后完成回读验证。
12. 设备断开后，界面和当前 Session 都得到一致状态。
13. Agent 请求用户完成一次重新上电操作并在确认后继续。
14. 从时间线回看完整的问题处理过程。
15. 导出一份包含产物、日志、点位和结论的验证报告。

---

## 18. 功能优先级

### P0：产品主链路

- 台架总览。
- 当前 Session 绑定。
- 统一任务状态。
- 台架时间线。
- 编译、日志、产物和下载。
- Modbus 点表、批量监控和趋势。
- 界面选中内容发送给 Agent。
- Agent 操作回显。
- 高影响操作确认。

### P1：增强日常效率

- 串口日志分析。
- 编译记录对比。
- 状态快照。
- 简单异常阈值。
- Agent 界面聚焦与标记。
- 人工操作请求。
- 轻量验证流程。

### P2：正式版完善

- 首次配置向导。
- 台架自检。
- 历史数据管理。
- Markdown 验证报告。
- 更完整的异常恢复和升级兼容。

---

## 19. 最终产品形态

完成本规划后，`dsh-vision-bench` 应具备以下体验：

- 用户打开 Windows 上的 DeepSeek Harness，进入一个工程 Session。
- 调试页和上位机页已经掌握当前工程、设备和连接状态。
- 用户可以像使用传统工具一样完成编译、下载、看日志和监控点位。
- 当前 Session 的台架模式 Agent 能随时查询同一份现场。
- 重要变化会以摘要通知 Agent，详细数据由工具按需获取。
- 用户可以把错误、日志、点位和曲线选区直接交给 Agent。
- Agent 的编译、查询、验证和请求会在传统界面中清晰呈现。
- 写点、下载和复位始终由用户掌握最终决定。
- 整个问题处理过程被时间线和证据串联起来。

最终目标可以概括为：

> 在一台 Windows 电脑上，把传统嵌入式调试、上位机监控和当前 Session Agent 变成同一套工作流，而不是三个互相隔离的工具。
